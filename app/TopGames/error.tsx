import Link from "next/link";
import React from "react";

type Props = {};

const error = (props: Props) => {
  return <Link href="/">ERROR from TopGames dir - Back to home</Link>;
};

export default error;
